'use strict';
import { environment } from './environments/environment';

export const paginationSkip: number = 10;
export const baseAPI: string = environment.api;
export const baseAPIToken: string = environment.apiToken;
export const baseAPIAnexo: string = environment.apiAnexo;